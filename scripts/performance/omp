#!/usr/bin/env python3
"""Deterministic OMP stand-in for transcript streaming performance profiles."""
from pathlib import Path
import os
import sys


if sys.argv[1:] == ["--version"]:
    print("10x-transcript-burst 1.1.0")
    raise SystemExit(0)

fixture = Path(__file__).resolve().parents[2] / "OmpKit/Tests/OmpKitTests/Fixtures/fake_server.py"
os.execv(sys.executable, [sys.executable, str(fixture), "transcript-burst", *sys.argv[1:]])
