# Tool details native acceptance

Status: native feature acceptance passed. Final production source: `36e16de72ab0ea5088a6674223a0928125a4edfa`; package and exact executable hash are in `final-corrections-package.json`.

## Verified in Release

- A 45-line controlled command advanced from 9.0 to 21.0 seconds while running. Expanded output showed the latest ten lines, 13–22, rather than the first ten. Completion showed 45.2 seconds. Show more exposed all 45 lines; Copy preserved all 45 when pasted into the native composer, then cleared without sending.
- The child card opened the reported nested session, displaying its two-message transcript. Its live recent-tool list and output were visible. Final history reconciliation originally removed those details; the correction now retains missing recent arrays from the same live child while accepting the recorded completion, result, and model fields. A new native run proved the three recent tools and latest output remain after completion.
- The real Cursor multi-file edit originally rendered an unavailable “Changed file” link in this older branch. Reusing the reviewed per-file result parser exposed both file paths. Clicking each diff-file action opened the corresponding `editable-alpha.txt` / `editable-beta.txt` tab in the selected Cursor app. Focused editor-panel AX excerpts record each selected tab and full neutral path components; unrelated Cursor panels were excluded.
- The preferred editor was selected in this isolated QA app's settings. No user profile setting or repository content was changed by editor navigation.
- Sending while scrolled up and session switching reuse the actual native acceptance in PR #35; those changes must be retained during stack integration.

## Checks and artifacts

The reused extractor correction passed 48 extractor/diff/resolver/editor checks. The recent-detail regression failed before the fix, then passed with six adjacent reconciliation/identity checks. Earlier timing, output-tail, editor, library, child-navigation, and approved snapshot checks are recorded in `.superpowers/sdd/tool-details/report.md`. No full suite was repeated.

Primary images: `native-running-early.jpg`, `native-running-tail.jpg`, `native-output-expanded.jpg`, `native-child-open.jpg`, `native-final-child-running.jpg`, `native-final-child-completed.jpg`, and `native-final-multifile-links.jpg`. The parent inspected the timer/tail, child-open, final child-completion, and multi-file screenshots. AX files omit provider-account rows. Both controlled runs and the exact current fixture are preserved.

## Limits

The controlled RPC fixture emits one-second cumulative output and recorded child-session data; it does not execute the displayed 45-line shell command or a live model child. Existing real-model tool history supplied the multi-file editor acceptance. Cold child history shows only recorded data; recent live progress is not fabricated after relaunch. The full audit stack remains separate feature builds, with later Stop/disclosure/scroll integration still required. No merge or deployment occurred. The Details QA app was closed after acceptance; the two neutral files were opened in Cursor for verification.
