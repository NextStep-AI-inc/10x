import SwiftUI

/// Placeholder until Task 12 rewrites install, permissions, and MCP mount settings.
struct ComputerUseSettingsSection: View {
    var body: some View {
        Text("Computer use settings arrive with the daemon install step")
            .font(TenXTypography.body(size: 13))
            .foregroundStyle(TenXPalette.color(TenXPalette.mutedTextHex))
            .frame(maxWidth: .infinity, alignment: .leading)
            .padding(.vertical, 8)
    }
}
